export const styles = theme => ({
  wrapper: {
    position: 'relative',
  },
  fab: {
    position: 'absolute',
    top: 0,
    right: 10,
  },
  root: {
    flexGrow: 1,
  },
  paper: {
    padding: theme.spacing(2),
    textAlign: 'center',
    color: theme.palette.text.secondary,
  },
});
